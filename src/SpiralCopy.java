public class SpiralCopy {
    
    public void spiralCopy(int[][] matrix){
        int top = 0, left = 0, right = matrix[0].length - 1, bottom = matrix.length - 1, count = 0;
        int[] result = new int[matrix[0].length * matrix.length];

        while(left <= right && top <=bottom){
            // left to right
            for(int i = left; i<= right; i++ ){
                
            }


            // copy top to bottom

            // if there's still a row to go through, copy right to left

            // if there's still a column, copy bottom to top
        }

    }
}
